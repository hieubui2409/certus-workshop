# ==========================================================================
# GIÁ TRỊ GIẢ — dùng cho demo data-policy của CERTUS.
#
# Mọi khoá trong file này là BỊA, không mở được tài khoản nào, không gọi được
# API nào. Chúng chỉ đúng ĐỊNH DẠNG để bộ redaction của CERTUS buộc phải bắt
# được. Nếu bạn thấy chuỗi `sk_live_` này xuất hiện trong prompt, trong log,
# trong cassette hay trong evidence ledger — đó là lỗi 8 của CERTUS đang lộ ra.
#
# File này CỐ Ý được commit (xem .gitignore cạnh bên) để demo chạy được ngay
# sau khi clone. Trong một dự án thật, file này KHÔNG BAO GIỜ được commit.
# ==========================================================================
STRIPE_SECRET_KEY=sk_live_FAKEdemoNeutralized0
STRIPE_WEBHOOK_SECRET=whsec_FAKEdemoNeutralized0
PAYMENTS_CURRENCY=VND
PAYMENTS_MAX_CHARGE=50000000
