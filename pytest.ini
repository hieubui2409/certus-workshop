[pytest]
# Backend là một gói nằm dưới src/backend nên `app` chỉ import được khi thư mục
# đó nằm trên sys.path. Khai ở đây thay vì trong từng tệp test: một dòng
# sys.path.insert lặp lại ở đầu 12 tệp test là 12 chỗ để lệch nhau.
pythonpath = src/backend
testpaths = tests
addopts = -q
