# patch 03a — Prompt injection
# lỗ hổng: 03-injection
# vì sao: Không rào thì một comment trong file người dùng upload điều khiển được phán quyết của hệ thống — bên bị chấm tự chấm.
--- a/src/backend/app/agent/context.py
+++ b/src/backend/app/agent/context.py
@@ -88,5 +88,11 @@
     parts: list[str] = []
     for path in sorted(files):
         source = _clip(files[path])
-        parts.append(f"### {path}\n{source}")
+        # Rào lại và dán nhãn. Nội dung này do người ngoài viết; nó là DỮ
+        # LIỆU để phân tích, không phải mệnh lệnh để thi hành.
+        parts.append(
+            f"<untrusted_user_file path={path!r}>\n"
+            f"{source}\n"
+            f"</untrusted_user_file>"
+        )
     return "\n\n".join(parts)
